<%- include('../partials/header') %>

<ul>
    <% for (let i = 0; i < songs.length; i++) { %>
        <%- include('../partials/song', { song: songs[i] }) %>
    <% } %>
</ul>

<%- include('../partials/footer') %>